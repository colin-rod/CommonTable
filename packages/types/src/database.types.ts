/**
 * Database types generated from Supabase schema
 *
 * Note: This file should be regenerated after database migrations using:
 * supabase gen types typescript --project-id <project-id> > packages/types/src/database.types.ts
 *
 * For now, we use the types defined in models.ts as the source of truth.
 */

// Placeholder - types are defined in models.ts
export type Database = Record<string, never>;
