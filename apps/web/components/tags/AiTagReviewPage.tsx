import { Container, Stack, Typography } from '@mui/material';

/**
 * AiTagReviewPage component (Placeholder)
 * TODO: Implement full AI tag review functionality with:
 * - Fetch all recipes with pending AI tag suggestions
 * - Group suggestions by recipe
 * - Accept/reject individual tags
 * - Batch accept/reject actions
 */
export function AiTagReviewPage() {
  return (
    <Container maxWidth="md">
      <Stack spacing={3}>
        <Typography variant="h5">AI Tag Review</Typography>
        <Typography variant="body2" color="text.secondary">
          This page is under construction
        </Typography>
      </Stack>
    </Container>
  );
}
